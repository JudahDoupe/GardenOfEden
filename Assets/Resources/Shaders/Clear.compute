#pragma kernel Clear

RWTexture2DArray<float4> Map;

static float TextureWidthInPixels = 512.0f; 

[numthreads(8, 8, 1)]
void Clear(uint3 id : SV_DispatchThreadID)
{
    [unroll]
    for (int i = 0; i < 6; ++i)
    {
        Map[int3(id.xy, i)] = float4(0,0,0,0);
    }
}
